{
  "name": "ross",
  "version": "1.0.0",
  "author": "Bababum",
  "license": "GPL-2.0-or-later",
  "scripts": {
    "git-push-theme": "git subtree split --prefix=theme -b theme && git push origin theme --force"
  }
}
