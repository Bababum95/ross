export type TextContentAttributes = {
  title: string;
  description: string;
  blockName?: boolean;
};

export type HilightType = {
  value: string;
  description: string;
};

export type LicenseType = {
  number: string;
  link: string;
};
